@using Velo.Models
@model IEnumerable<RELATION>

    @*<div class="card">
        <div class="card__icon blur-image">
            <img class="card__image" src="/assets/img/b1.jpg" />
        </div>
        <div class="card__body">
            <p>Bạn có thêm một lượt thích mới</p>
        </div>
    </div>
    <div class="card">
        <div class="card__icon blur-image">
            <img class="card__image" src="/assets/img/b2.jpg" />
        </div>
        <div class="card__body">
            <p>Bạn có thêm một lượt thích mới</p>
        </div>
    </div>*@
    @foreach (RELATION rela in Model)
    {
        if (rela.ACCOUNT1.isVip)
        {
            <div class="card">
                <div class="card__icon">
                    <img class="card__image" src="@rela.ACCOUNT.Photos.Where(p => p.isAvatar == true).FirstOrDefault().Link" />
                </div>
                <div class="card__body">
                    <p>Bạn có thêm một lượt thích mới</p>
                </div>
            </div>
        }
        else
        {
            <div class="card">
                <div class="card__icon blur-image">
                    <img class="card__image" src="@rela.ACCOUNT.Photos.Where(p => p.isAvatar == true).FirstOrDefault().Link" />
                </div>
                <div class="card__body">
                    <p>Bạn có thêm một lượt thích mới</p>
                </div>
            </div>
        }
    }
